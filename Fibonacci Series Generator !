#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N;
    scanf("%d", &N);  

    int a = 0, b = 1, c;

    if (N <= 0) {
        return 0; 
    }

    printf("%d", a);

    for (int i = 1; i < N; i++) {
        printf(" %d", b);
        c = a + b;
        a = b;
        b = c;
    }
    
    return 0;
}
