#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N, sum = 0;
    scanf("%d", &N);  

    if (N < 0) {
        N = -N;
    }

    while (N != 0) {
        sum += N % 10;  
        N = N / 10;     
    }

    printf("%d", sum);
    
    return 0;
}
